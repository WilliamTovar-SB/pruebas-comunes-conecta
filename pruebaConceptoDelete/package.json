{
    "name": "prueba-concepto",
    "description": "prueba-concepto-description",
    "version": "0.0.1",
    "main": "index.js",
    "keywords": [],
    "author": "William A Tovar R - Seguros Bolivar",
    "license": "ISC",
    "dependencies": {
        "aws-sdk": "^2.799.0",
        "uuid": "^8.3.2",
        "axios": "^0.21.1",
        "moment": "^2.29.1",
        "form-data": "^4.0.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.64",
        "@types/jest": "^26.0.20",
        "@types/node": "^14.14.21",
        "@types/uuid": "^8.3.4",
        "aws-sdk": "^2.778.0",
        "jest": "^26.6.1",
        "rimraf": "^3.0.2",
        "ts-jest": "^26.4.3",
        "ts-sinon": "^2.0.1",
        "typescript": "^4.0.3"
    },
    "scripts": {
        "test:one": "jest unitTests.spec.ts",
        "test": "jest --detectOpenHandles --forceExit",
        "test:watch": "jest --watch",
        "test:coverage": "jest --coverage --no-cache",
        "build": "npm run clean && tsc -p .",
        "clean": "rimraf dist && rimraf release",
        "postbuild": "cp package.json dist/package.json && cd dist && npm install --only=production",
        "upload": "mkdir release && zip prueba-concepto.zip dist -r && mv prueba-concepto.zip release",
        "local": "aws s3 cp release/prueba-concepto.zip s3://sarlaft-lambda/prueba-concepto.zip"
    }
}

